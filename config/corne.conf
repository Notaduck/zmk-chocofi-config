# Display
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# nice-view-gem: no animation, fixed frame on the peripheral
CONFIG_NICE_VIEW_GEM_ANIMATION=n
CONFIG_NICE_VIEW_GEM_ANIMATION_FRAME=1

# Sleep 45m * 60s * 1000ms = 2700000
CONFIG_ZMK_SLEEP=y # Enable deep sleep support
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=2700000

# Bluetooth
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

CONFIG_ZMK_KEYBOARD_NAME="Chocofi"

# nice-oled on the right half: one static spaceman frame, no animation
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL=n
CONFIG_NICE_OLED_WIDGET_STATIC_IMAGE_PERIPHERAL_SPACEMAN=y

# Battery savers: wake the CPU 10x less for LVGL housekeeping, report
# battery (and redraw its widget) every 5 minutes instead of every 60s
CONFIG_ZMK_DISPLAY_TICK_PERIOD_MS=100
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=300
